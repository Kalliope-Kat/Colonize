var options = {
    display: function(){
        saveButton.x = 1220;
        saveButton.y = 930;
        
        quitButton.x = 1520;
        quitButton.y = 930;
        
        stage.addChild(saveButton);
        stage.addChild(quitButton);
    }
}